# Configuration

The plugin accepts overrides for:

- `scales`
- `viewportMin`
- `viewportMax`
- `textTypes`
- `fontFamilies`
- `colors`
- `opacitySteps`

Use this when you want to pre-generate a tighter scale or add custom semantic text types.
